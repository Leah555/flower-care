# 花卉养护记录系统 - 数据库结构改进方案

## 项目需求回顾

**基本任务要求：**
1. 花卉基本信息：名称、品种、种植日期、浇水频率、施肥时间与用量、病虫害防治情况
2. 养护操作记录：浇水、施肥、病虫害防治等详细信息
3. 查询功能：按花卉名称或品种查询养护记录
4. 养护提醒：浇水、施肥等提醒设置

**提升任务要求：**
1. 数据对比分析：依据养护记录与生长状况，总结养护经验
2. 为同品种花卉养护提供优化方案建议

## 当前表结构的不足

### 1. 花卉基本信息表 (plants) 缺失字段
- 施肥频率（只有浇水频率）
- 光照需求（重要养护参数）
- 温度范围（适宜生长温度）
- 湿度要求（适宜湿度范围）
- 病虫害历史记录（用于数据分析）

### 2. 养护操作记录表 (care_records) 不够细化
- 操作类型需要更详细分类（施肥类型、病虫害类型等）
- 缺少生长状态记录
- 缺少病虫害防治的具体信息

### 3. 缺少数据分析支持表
- 没有专门记录生长状况的表
- 缺少养护经验总结表
- 缺少品种养护建议表

## 改进后的数据库表结构

### 1. 花卉基本信息表 (plants) - 增强版
```sql
CREATE TABLE plants (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(50) NOT NULL,           -- 花卉名称
    variety VARCHAR(50) NOT NULL,        -- 品种（改为NOT NULL）
    planting_date DATE NOT NULL,         -- 种植日期（改为NOT NULL）
    
    -- 养护参数
    water_frequency INTEGER,             -- 浇水频率（天）
    fertilize_frequency INTEGER,         -- 施肥频率（天）
    light_requirement VARCHAR(20),       -- 光照需求：强/中/弱
    temperature_min INTEGER,             -- 最低适宜温度
    temperature_max INTEGER,             -- 最高适宜温度
    humidity_requirement VARCHAR(20),    -- 湿度要求：高/中/低
    
    -- 状态跟踪
    last_water_date DATE,                -- 最后浇水日期
    last_fertilize_date DATE,            -- 最后施肥日期
    last_pest_control_date DATE,         -- 最后病虫害防治日期
    status VARCHAR(20) DEFAULT '正常',   -- 生长状态
    health_score INTEGER DEFAULT 100,    -- 健康评分（0-100）
    
    -- 统计分析字段
    total_care_operations INTEGER DEFAULT 0, -- 总养护操作次数
    pest_incidents INTEGER DEFAULT 0,    -- 病虫害发生次数
    
    notes TEXT                           -- 备注信息
);
```

### 2. 养护操作记录表 (care_records) - 细化版
```sql
CREATE TABLE care_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    plant_id INTEGER NOT NULL,           -- 关联的花卉ID
    
    -- 操作类型细化
    operation_category VARCHAR(20) NOT NULL, -- 操作类别：浇水/施肥/病虫害/修剪/换盆
    operation_type VARCHAR(30) NOT NULL, -- 具体类型：
                                         -- 浇水：普通浇水/叶面喷水
                                         -- 施肥：有机肥/化肥/叶面肥
                                         -- 病虫害：预防/治疗
                                         -- 修剪：整形/枯枝/花后修剪
                                         -- 换盆：换盆/添土
    
    operation_date DATETIME NOT NULL,    -- 操作时间
    
    -- 操作详情
    details TEXT,                        -- 操作详情描述
    amount VARCHAR(20),                  -- 用量（如水量、肥料量）
    duration_minutes INTEGER,            -- 操作耗时（分钟）
    
    -- 效果记录
    plant_condition_before VARCHAR(20),  -- 操作前植物状态
    plant_condition_after VARCHAR(20),   -- 操作后植物状态
    effectiveness_rating INTEGER,        -- 效果评分（1-5星）
    
    -- 病虫害相关（如果是病虫害操作）
    pest_type VARCHAR(30),               -- 病虫害类型
    control_method VARCHAR(30),          -- 防治方法
    pesticide_name VARCHAR(50),          -- 药剂名称
    pesticide_dosage VARCHAR(20),        -- 药剂用量
    
    FOREIGN KEY (plant_id) REFERENCES plants(id)
);
```

### 3. 养护提醒设置表 (reminders) - 增强版
```sql
CREATE TABLE reminders (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    plant_id INTEGER NOT NULL,
    
    reminder_type VARCHAR(30) NOT NULL,  -- 提醒类型扩展：
                                         -- 浇水/施肥/病虫害检查/修剪/换盆/季节调整
    
    frequency INTEGER NOT NULL,          -- 频率（天）
    seasonal_adjustment BOOLEAN DEFAULT 0, -- 是否季节调整
    spring_frequency INTEGER,            -- 春季频率
    summer_frequency INTEGER,            -- 夏季频率
    autumn_frequency INTEGER,            -- 秋季频率
    winter_frequency INTEGER,            -- 冬季频率
    
    last_reminder_date DATE,             -- 最后提醒时间
    next_reminder_date DATE,             -- 下次提醒时间
    is_active BOOLEAN DEFAULT 1,         -- 是否激活
    
    priority INTEGER DEFAULT 1,          -- 提醒优先级（1-5）
    
    FOREIGN KEY (plant_id) REFERENCES plants(id)
);
```

### 4. 新增：生长记录表 (growth_records)
```sql
CREATE TABLE growth_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    plant_id INTEGER NOT NULL,
    record_date DATE NOT NULL,           -- 记录日期
    
    -- 生长指标
    height_cm DECIMAL(5,2),              -- 高度（厘米）
    leaf_count INTEGER,                  -- 叶片数量
    bud_count INTEGER,                   -- 花蕾数量
    flower_count INTEGER,                -- 开花数量
    
    -- 健康状况
    health_score INTEGER,                -- 健康评分
    leaf_color VARCHAR(20),              -- 叶色
    growth_vigor VARCHAR(20),            -- 生长活力
    
    -- 环境因素
    temperature INTEGER,                 -- 环境温度
    humidity INTEGER,                    -- 环境湿度
    light_exposure VARCHAR(20),          -- 光照情况
    
    notes TEXT,                          -- 备注
    photo_path VARCHAR(200),             -- 照片路径（可选）
    
    FOREIGN KEY (plant_id) REFERENCES plants(id)
);
```

### 5. 新增：养护经验表 (care_experience)
```sql
CREATE TABLE care_experience (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    variety VARCHAR(50) NOT NULL,        -- 花卉品种
    
    -- 成功经验
    optimal_water_frequency INTEGER,     -- 最佳浇水频率
    optimal_fertilize_frequency INTEGER, -- 最佳施肥频率
    best_season VARCHAR(20),             -- 最佳生长季节
    common_pests TEXT,                   -- 常见病虫害
    effective_controls TEXT,             -- 有效防治方法
    
    -- 失败教训
    common_mistakes TEXT,                -- 常见养护错误
    warning_signs TEXT,                  -- 问题预警信号
    recovery_methods TEXT,               -- 恢复方法
    
    -- 统计分析
    total_plants INTEGER DEFAULT 0,      -- 总养护数量
    success_rate DECIMAL(5,2),           -- 成功率
    avg_health_score DECIMAL(5,2),       -- 平均健康评分
    
    last_updated DATE,                   -- 最后更新日期
    confidence_level INTEGER DEFAULT 1   -- 经验可信度（1-5）
);
```

## 改进后的优势

### 1. 更好的数据分析支持
- 生长记录表支持趋势分析
- 养护经验表支持品种对比
- 详细的养护效果记录

### 2. 更全面的养护管理
- 季节调整的提醒机制
- 病虫害防治的详细记录
- 养护效果的量化评估

### 3. 提升任务支持
- 数据对比：通过生长记录和养护效果进行对比
- 经验总结：通过养护经验表积累知识
- 优化建议：基于统计分析提供养护建议

## 实施建议

1. **分阶段实施**：先实现基本表结构，再逐步添加高级功能
2. **向后兼容**：保持现有API接口，新增功能作为扩展
3. **数据迁移**：提供从旧表到新表的数据迁移工具
4. **界面适配**：根据新表结构调整用户界面

这个改进方案能够更好地支持项目需求，特别是提升任务中的数据分析和经验总结功能。